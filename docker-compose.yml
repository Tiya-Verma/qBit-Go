services:
  qbit:
    build: .
    ports:
      - "8080:8080"
      - "6881:6881"
      - "6881:6881/udp"
    volumes:
      - ./downloads:/downloads
    command: ["./qbit", "--port", "8080", "--listen-port", "6881", "--download-dir", "/downloads"]
